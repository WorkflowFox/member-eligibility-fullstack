# Dev-only / generated files excluded from the docker build context.
# See apps/api/Dockerfile (#8) -- the image must not include these.

__pycache__/
*.pyc
.pytest_cache/
.venv/

tests/
requirements-dev.txt
pytest.ini

# Locally generated by seed.py -- the image bakes its own copy in during
# `docker build` (see Dockerfile), not whatever happens to exist locally.
data/

.git
.gitignore
.dockerignore
Dockerfile
